import 'package:flutter/material.dart';

class Characters_details extends StatelessWidget {
  const Characters_details({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("details"),
    );
  }
}